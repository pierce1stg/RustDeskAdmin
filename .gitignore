# Dependencies
node_modules/
# Go modules resolve at build. NOTE: narrow rule on purpose — the small
# frontend vendored lib (tweetnacl) IS tracked, so no bare vendor/ line.
backend/vendor/

# Build outputs
dist/
build/
*.exe
*.dll
*.so
*.dylib

# Go workspace (unused; kept if ever re-enabled)
go.work
go.work.sum

# Docker
.docker/
docker-compose.override.yml

# Environment
.env
.env.local
.env.*.local
.env.example.local

# Local-only tooling (kept off GitHub)
scripts/check-anonymity.sh

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Database
*.sqlite
*.sqlite3
*.db

# Test env (isolated install; configs are tracked)
frontend/.vitest-env/node_modules/
frontend/.vitest-env/.playwright/
frontend/.vitest-env/.vite-cache/

# Temporary
tmp/
temp/
*.tmp
*.out
*.tgz
*.tar.gz
*.zip
*MERGE*.py
SETTINGS_MERGE*
# Local backups / dumps (never public)
*.bak
*.backup
*.orig
dump.sql
backup.sql
db.dump
*.sqlite-journal
*.db-journal
id_rsa
id_ed25519
*.p12

# Build artifacts
backend/bin/
backend/tmp/
frontend/dist/

# Keys / certs
*.pem
*.key
*.crt
*.cert

# Terraform
*.tfstate
*.tfstate.backup

# Runtime data (created by setup.sh)
data/
status/
# local release artifacts
.release/
